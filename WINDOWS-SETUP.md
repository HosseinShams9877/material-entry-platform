# راهنمای اجرای محلی روی ویندوز

**سامانهٔ مدیریت تولید و خدمات پس از فروش تجهیزات پزشکی (MD-MES) — نسخهٔ ۱٫۰٫۰**

این راهنما نصب و اجرای کامل سامانه را روی **ویندوز ۱۰ / ۱۱** به‌صورت محلی (لوکال) گام‌به‌گام توضیح می‌دهد. تمام دستورها هم برای **PowerShell** و هم **CMD** کار می‌کنند. اگر با لینوکس یا مک کار می‌کنید، همان گام‌ها را با دستورهای `README.md` اصلی اجرا کنید.

> 💡 **خلاصهٔ سریع (۴ دستور):**
>
> ```powershell
> cd C:\medical-device-mes
> npm install
> npx prisma generate
> npm run dev
> ```
>
> سپس در مرورگر: `http://localhost:3000` — ورود با `admin` / `demo1234`

---

## ۱. پیش‌نیازها

| نرم‌افزار | حداقل نسخه | نسخهٔ پیشنهادی | محل دریافت |
|-----------|-------------|-----------------|-------------|
| **Node.js** (همراه npm) | 20.9 | 22 LTS | [nodejs.org](https://nodejs.org) |
| مرورگر مدرن | — | Chrome / Edge / Firefox جدید | — |
| Git برای ویندوز | اختیاری | آخرین نسخه | [git-scm.com](https://git-scm.com) |

پس از نصب Node.js، یک پنجرهٔ ترمینال جدید باز کنید و صحت نصب را بررسی کنید:

```powershell
node -v     # باید v20.9 یا بالاتر باشد (مثلاً v22.x.x)
npm -v      # باید شمارهٔ نسخه چاپ شود (مثلاً 10.x.x)
```

> ⚠️ اگر پس از نصب Node.js دستور `node` شناخته نشد، ترمینال را ببندید و دوباره باز کنید تا مسیرها (PATH) تازه شوند.

**نکته‌های مهم دربارهٔ پیش‌نیازها:**

1. به هیچ پایگاه‌دادهٔ خارجی (SQL Server، MySQL و…) نیاز ندارید — پایگاه‌دادهٔ **SQLite** به‌صورت فایل همراه پروژه است.
2. فقط در یک نقطه اینترنت لازم است: هنگام `npm install` (دریافت بسته‌ها). فونت وزیرمتن به‌صورت لوکال داخل خود پروژه است (`public/fonts`) و بعد از نصب، سامانه کاملاً آفلاین کار می‌کند.
3. نیازی به اجرای ترمینال به‌عنوان Administrator نیست؛ اجرای معمولی کافی است.
4. نصب Git اختیاری است و فقط اگر بخواهید از «Git Bash» استفاده کنید لازم است.

---

## ۲. استخراج بسته در مسیر کوتاه

فایل `medical-device-mes.zip` را استخراج کنید. **مسیر استخراج پیشنهادی:**

```text
C:\medical-device-mes
```

در PowerShell می‌توانید استخراج را همین‌جا انجام دهید (فرض: فایل زیپ در پوشهٔ Downloads است):

```powershell
Expand-Archive -Path "$env:USERPROFILE\Downloads\medical-device-mes.zip" -DestinationPath "C:\" -Force
cd C:\medical-device-mes
```

**چرا مسیر کوتاه و ساده مهم است؟**

- ویندوز برای مسیرهای طولانی (بیش از ~۲۶۰ نویسه) خطای «Path too long» می‌دهد؛ پوشهٔ `node_modules` عمق زیادی دارد.
- مسیرهایی که از OneDrive یا Dropbox عبور می‌کنند، به‌دلیل همگام‌سازی مداوم، نصب را بسیار کُند و گاهی خراب می‌کنند.
- مسیرهای دارای فاصله یا نویسهٔ فارسی می‌توانند در برخی ابزارها مشکل ایجاد کنند؛ مسیری مثل `C:\Users\حسین\Desktop\پروژه من` توصیه نمی‌شود.

اگر مجبورید پوشه را جای دیگری بگذارید، فقط مراقب این سه مورد باشید.

---

## ۳. فعال‌سازی اجرای npm در PowerShell (فقط بار اول)

سیاست پیش‌فرض ویندوز گاهی اجرای اسکریپت‌های npm در PowerShell را با پیام `running scripts is disabled on this system` می‌بندد. برای رفع آن (یک‌بار برای همیشه، فقط برای کاربر جاری):

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

به سؤال تأیید، `Y` بدهید و Enter بزنید.

> اگر از **CMD** یا **Git Bash** استفاده می‌کنید، این گام لازم نیست.
> اگر همین حالت `npm -v` در PowerShell جواب می‌دهد، این گام را رد کنید.

---

## ۴. گام‌های نصب و اجرا

### گام ۱ — ورود به پوشهٔ پروژه

```powershell
cd C:\medical-device-mes
```

### گام ۲ — نصب بسته‌ها

```powershell
npm install
```

این مرحله چند دقیقه طول می‌کشد (بسته به سرعت اینترنت). در پایان نباید خطایی با کلمهٔ `ERR!` ببینید — هشدارهای `warn` طبیعی‌اند.

### گام ۳ — ساخت فایل تنظیمات (.env)

فایل `.env.example` را به `.env` کپی کنید:

```powershell
Copy-Item .env.example .env      # PowerShell
```

یا در CMD:

```cmd
copy .env.example .env
```

سپس برای بررسی/ویرایش آن را در Notepad باز کنید:

```powershell
notepad .env
```

محتوای پیش‌فرض چنین است و **بدون تغییر هم کار می‌کند**:

```ini
DATABASE_URL="file:../db/custom.db"
```

اگر به هر دلیلی پایگاه‌داده پیدا نشد (خطای `Unable to open database`)، مسیر مطلق را جایگزین کنید — دقت کنید **به‌جای `\` از `/` استفاده کنید**:

```ini
DATABASE_URL="file:C:/medical-device-mes/db/custom.db"
```

> ⚠️ **دقت:** مسیرهای نسبی در Prisma نسبت به پوشهٔ `prisma` (محل فایل schema) تفسیر می‌شوند؛ به همین دلیل شکل صحیح نسبی `file:../db/custom.db` است و `file:./db/custom.db` **نادرست** است. در صورت تردید، همیشه مسیر مطلق با اسلش (`/`) بنویسید.

### گام ۴ — تولید کلاینت Prisma

```powershell
npx prisma generate
```

خروجی موفق با عبارت `Generated Prisma Client` پایان می‌یابد.

### گام ۵ — اجرای سامانه

```powershell
npm run dev
```

پس از چند ثانیه پیام `✓ Ready in ...s` ظاهر می‌شود. مرورگر را باز کنید:

```text
http://localhost:3000
```

صفحهٔ ورود با نام کاربری `admin` و رمز عبور `demo1234` باز می‌شود.

**متوقف‌کردن سرور:** در پنجرهٔ ترمینال کلیدهای `Ctrl + C` را بزنید و به سؤال تأیید (در PowerShell) `Y` بدهید.

> 💡 اولین بارگذاریٔ صفحه ممکن است کمی طول بکشد (کامپایل اولیه)؛ در بارهای بعد بسیار سریع می‌شود.
>
> 💡 اگر ویندوز برای نخستین اجرا پیام **فایروال** نمایش داد، گزینهٔ Allow (شبکه‌های خصوصی) را انتخاب کنید — این پیام فقط برای دسترسی same-machine است و نگرانی امنیتی ایجاد نمی‌کند.

---

## ۵. حساب‌های نمایشی (همگی با رمز `demo1234`)

| نام کاربری | نقش | دسترسی اصلی |
|-----------|-----|-------------|
| `admin` | مدیر سیستم | همهٔ ماژول‌ها + مدیریت کاربران + پشتیبان‌گیری |
| `pmgr` | مدیر تولید | درخواست/تأیید/شروع تولید، کانبان، انبارش و ارسال |
| `operator` | اپراتور تولید | ثبت مراحل تولید، ثبت نتایج تست |
| `qc` | مسئول کنترل کیفیت | IQC، تأیید تست، NCR، آزادسازی محصول |
| `warehouse` | انباردار | دریافت مواد، ورود/خروج دستی، مصرف BOM، دفتر گردش |
| `smgr` | مدیر خدمات پس از فروش | تیکت‌ها، شکایات، تعمیرات |
| `tech1` | تکنسین خدمات | عیب‌یابی، تعمیر، مصرف قطعه |
| `sales` | واحد فروش | مشتریان، سفارش مشتری، تحویل دستگاه |
| `viewer` | مشاهده‌گر | فقط گزارش‌ها و نماها (بدون تغییر) |

پایگاه‌دادهٔ همراه بسته از پیش با داده‌های نمایشی پر شده است (محصولات، ۳ سفارش تولید در وضعیت‌های مختلف، دستگاه‌ها، تیکت‌ها، انبار و…)، بنابراین بلافاصله بعد از اجرا می‌توانید همهٔ ماژول‌ها را ببینید و امتحان کنید.

---

## ۶. اجرا در حالت Production (اختیاری)

برای اجرای بهینه‌شده (شبیه استقرار واقعی) به‌جای حالت توسعه:

```powershell
npm run build
npm run start
```

- `npm run build` چند دقیقه طول می‌کشد و نسخهٔ بهینهٔ سامانه را می‌سازد.
- `npm run start` سامانه را روی پورت ۳۰۰۰ بالا می‌آورد (همان `http://localhost:3000`).
- برای هر تغییر در کد، ابتدا `Ctrl+C`، سپس مجدداً `build` و `start` لازم است.

---

## ۷. ساخت پایگاه‌دادهٔ تمیز (به‌جای دادهٔ نمونه)

اگر می‌خواهید با پایگاه‌دادهٔ خالی شروع کنید (مثلاً برای ورود دادهٔ واقعی خودتان):

```powershell
# توقف سرور در صورت اجرا (Ctrl+C) سپس:
del db\custom.db          # PowerShell
npm run db:push           # ساخت جداول از روی schema
npm run db:seed           # درج دادهٔ نمایشی (اختیاری — برای خالی واقعی، این دستور را اجرا نکنید)
```

> ابزار `tsx` موردنیاز seed از قبل در وابستگی‌های پروژه (`devDependencies`) قرار دارد و همراه `npm install` نصب می‌شود.

**پشتیبان‌گیری از داده‌ها:** کافی است از فایل `db\custom.db` نسخهٔ کپی تهیه کنید (زمانی که سرور اجرا نیست یا حداقل در حال نوشتن نیست). بازگردانی هم یعنی جایگزینی همان فایل. از داخل خود سامانه نیز ماژول «مدیریت سامانه ← پشتیبان‌گیری» فایل پشتیبان تهیه می‌کند.

---

## ۸. رفع اشکال‌های رایج ویندوز

| مشکل | راه‌حل |
|------|--------|
| پیام `npm : File ...npm.ps1 cannot be loaded because running scripts is disabled` | گام ۳ همین راهنما: `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` |
| خطای `Error: listen EADDRINUSE: port 3000` (پورت اشغال) | سرور را روی پورت دیگر بالا بیاورید: `npx next dev -p 3001` — یا فرایند قبلی را ببندید: `netstat -ano \| findstr :3000` سپس `taskkill /PID <شماره> /F` |
| خطای Prisma: `Unable to open database file` یا جدول‌های خالی | در `.env` مسیر مطلق را با اسلش قرار دهید: `DATABASE_URL="file:C:/medical-device-mes/db/custom.db"` سپس `npx prisma generate` و ری‌استارت سرور |
| پیام `@prisma/client did not initialize yet` | `npx prisma generate` را اجرا و سپس سرور توسعه را با `Ctrl+C` متوقف و دوباره `npm run dev` کنید |
| خطای `File ... path too long` هنگام نصب | پروژه را در مسیر کوتاه‌تری استخراج کنید (گام ۲)؛ در ویندوز ۱۰ می‌توانید «Enable Win32 long paths» را از Group Policy فعال کنید |
| نصب خیلی کُند یا خطای `EPERM`/`ENOENT` هنگام `npm install` | پوشهٔ پروژه را از پوشه‌های همگام‌شوندهٔ OneDrive/Dropbox خارج کنید و پوشهٔ پروژه را در استثنائات آنتی‌ویروس/Defender قرار دهید؛ سپس پوشهٔ `node_modules` را پاک و دوباره `npm install` کنید |
| صفحه با فونت ناخوانا | فونت وزیرمتن داخل پروژه است (`public/fonts`)؛ اگر مرورگر قدیمی است، فونت جایگزین سیستم (Tahoma) استفاده می‌شود و سامانه همچنان کار می‌کند |
| تغییرات کد اعمال نمی‌شود | کش را پاک کنید: پوشهٔ `.next` را حذف و `npm run dev` را دوباره اجرا کنید |
| خطای `tsx not found` هنگام seed | `npm install -D tsx` و سپس `npm run db:seed` |

---

## ۹. پرسش‌های متداول

**۱. آیا می‌توانم از سند دسکتاپ استفاده کنم؟**
بهتر است ابتدا آن را به مسیری مثل `C:\medical-device-mes` منتقل یا کپی کنید تا مشکلات مسیر طولانی/فاصله/OneDrive پیش نیاید.

**۲. چند نفر هم‌زمان روی یک سیستم می‌توانند کار کنند؟**
حالت لوکال برای تک‌کاربر و ارزیابی است. استفادهٔ چندکاربره در شبکهٔ محلی مستلزم استقرار سروری (در یک سیستم مرکزی با `npm run build` و `npm run start`) و دسترسی بقیه از طریق IP همان سیستم است که از محدودهٔ این راهنما خارج است.

**۳. داده‌ها کجا ذخیره می‌شوند؟**
همه‌چیز در فایل `db\custom.db` (SQLite) به‌همراه فایل‌های پشتیبان در پوشهٔ `backups\` (خروجی ماژول پشتیبان‌گیری داخل سامانه).

**۴. آیا بعد از نصب، دوباره به اینترنت نیاز هست؟**
خیر؛ حتی فونت وزیرمتن هم به‌صورت لوکال داخل پروژه است و سامانه کاملاً آفلاین اجرا می‌شود.

**۵. نحوهٔ آپدیت به نسخهٔ جدید چیست؟**
بستهٔ جدید را استخراج کنید، پوشهٔ `db` نسخهٔ قبلی را جایگزین کنید (برای حفظ داده‌ها)، سپس `npm install` و `npx prisma db push` (برای اعمال تغییرات ساختاری احتمالی) و در پایان `npx prisma generate`.

---

*تهیه‌شده برای بستهٔ `medical-device-mes.zip` — نسخهٔ ۱٫۰٫۰. راهنمای کامل قابلیت‌ها، معماری و گردش‌کارها در فایل `README.md` همراه بسته.*
