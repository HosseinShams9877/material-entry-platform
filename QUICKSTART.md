# سامانهٔ هوشمند ثبت ورود مصالح کارگاه

پلتفرم موبایل‌اول و **کاملاً معمولی** برای کارگاه ساختمانی: ثبت ورود مصالح، **صورت وضعیت با امضای مدیر کل**، **گزارش کار کارگران و خوداظهاری نیروی اجرایی**، **اعلام نیاز → تأیید مدیر → کارپرداز و فاکتور → انبار → پرداخت و تسویه**، **مالی ساده (پرداخت/سررسید/بدهی/طلب)**، **ارزیابی و KPI نیروها**، **انبار کارگاه (موجودی/گردش/کمبود)**، **جابجایی نیرو با سابقه**، **چک‌لیست پروژه**، ماژول وظایف و گزارش روزانه، تقویم شمسی پاپ‌آپ و رابط فارسی راست‌به‌چپ — مطابق سند PDF «سیستم مدیریت یکپارچه» (ملاک رسمی تغییرات).

> **توجه:** ماژول صوت (ضبط پیام صوتی و تبدیل ویس به نوشتار) به‌درخواست کارفرما **به‌طور کامل حذف شده است** — هیچ وابستگی خارجی/هوش مصنوعی وجود ندارد؛ گزارش‌ها و دیدگاه‌ها متنی هستند. با تعیین سند PDF به‌عنوان ملاک، **فاز مالی سادهٔ سند (پرداخت/سررسید/بدهی/طلب + نقش حسابدار)** اضافه شد — بدون دفتر کل و اسناد حسابداری، با طراحی عامدانه ساده.

## راه‌اندازی سریع

پیش‌نیاز: [Bun](https://bun.sh) (یا Node.js 20+ با npm/pnpm)

```bash
bun install                # نصب وابستگی‌ها
bun run db:generate        # تولید Prisma Client
bun run db:deploy          # اعمال Migrationها (تولید) — یا db:push برای توسعه
bun run dev                # اجرای محیط توسعه روی http://localhost:3000
```

دیتابیس آماده (`db/custom.db`) به‌همراه داده‌های اولیه ضمیمه است؛ در صورت نیاز به دادهٔ تازه:

```bash
bunx tsx scripts/seed.ts   # بازسازی داده‌های اولیه (شامل نقش مدیر کل و Permissionهای جدید)
```

## حساب‌های آزمایشی (گذرواژهٔ همه: `123456`)

| حساب | نقش | دسترسی |
|------|-----|--------|
| `admin` | مدیر ارشد سیستم | همه‌جا |
| `gm.sa` | **مدیر کل** | امضای صورت وضعیت‌های بزرگ + مشاهدهٔ همه‌جا + **ثبت پرداخت/طلب** |
| `manager.sa` | مدیر پروژه | پروژه‌های سعادت‌آباد + بررسی/بستن/Rollback + تأیید خرید + چک‌لیست + **اهداف KPI** |
| `warehouse.sa` | انباردار | تأیید انبار و تحویل + **اعلام نیاز و دریافت خرید** + **انبار** |
| `purchaser.sa` | **کارپرداز / مسئول خرید** | **ثبت خرید با تأمین‌کننده/فاکتور/سررسید** + مشاهدهٔ انبار |
| `accountant.sa` | **حسابدار / مدیر مالی** | **مالی: پرداخت‌ها، بدهی‌ها، سررسید، طلب** + مشاهدهٔ خرید/انبار/ارزیابی |
| `worker.sa` | **نیروی اجرایی** | **خوداظهاری گزارش کار** (متصل به پروفایل کارگر «علی») |
| `inspector.sa` | ناظر کنترل کیفیت | بررسی فنی |
| `supervisor.sa` | سرپرست سعادت‌آباد | ثبت/گزارش کار/وظایف/صورت وضعیت |
| `supervisor.da` | سرپرست دروس | ثبت/گزارش کار/وظایف/صورت وضعیت |
| `viewer.sa` | بیننده | فقط مشاهده (Scope خودش) |

## تست‌ها (مجموعاً ۳۷۰)

```bash
node scripts/finance-tests.mjs         # ۶۳ تست مالی ساده + ارزیابی/KPI (جدید)
node scripts/proposal-tests.mjs        # ۵۹ تست سیستم یکپارچه (خرید/فاکتور/انبار/جابجایی/چک‌لیست/خوداظهاری)
bun scripts/modules-tests.mjs          # ۶۳ تست صورت وضعیت + خرید + گزارش کار
bun scripts/security-tests.mjs         # ۳۸ تست امنیتی (IDOR، قفل ۲۴ ساعته، CSRF، ...)
bun scripts/daily-tasks-tests.mjs      # ۵۹ تست ماژول وظایف/گزارش روزانه + گارد حذف کامل صوت
bun scripts/enterprise-tests.mjs       # ۵۶ تست زنجیرهٔ Workflow + RBAC + Idempotency
bun scripts/production-tests.mjs       # ۲۷ تست سخت‌سازی تولید
bun scripts/delivery-date-tests.mjs    # ۵ تست تاریخ ورود مصالح (جلالی)
```

> بین سوئیت‌ها یک‌بار سرور را ری‌استارت کنید (Rate Limit حافظه‌ای بین پروسه‌ای است).

## ماژول‌های کلیدی (طبق نیاز کارفرما)

### صورت وضعیت + امضای مدیر کل
- ثبت عنوان/دوره/مبلغ (تومان) با تقویم شمسی؛ ارسال → تأیید مدیر
- **مبلغ بالای ۱۵۰٬۰۰۰٬۰۰۰ تومان → پس از تأیید مدیر، امضای دستی مدیر کل لازم است** (پد امضای لمسی، ذخیرهٔ PNG خصوصی با Signed URL)
- زیر آستانه: تأیید مدیر نهایی است. مدیریت رد با ثبت علت. کارت‌ها در خانهٔ سرپرست، داشبورد مدیر و منوی پروفایل

### اعلام نیاز و کنترل خرید
- انباردار: «اعلام نیاز» (اقلام + مقدار + واحد) → مدیر: تأیید/رد → **کارپرداز: «ثبت خرید»** (تأمین‌کننده + تاریخ تحویل + مبلغ + سررسید + شرایط پرداخت + **تصویر فاکتور**) → انباردار: «دریافت در انبار» → **ورود خودکار به موجودی انبار**
- درخواست PENDING بیش از ۲۴ ساعت → بج «بی‌پاسخ ۲۴ ساعت» در فهرست و داشبورد

### مالی ساده — پرداخت، سررسید و بدهی (فاز ۳ سند PDF)
- صفحهٔ «مالی» با تب‌های بدهی‌ها/پرداخت‌ها/دریافت‌ها + کارت‌های بدهی کل، سررسید گذشته، پرداخت‌شده، طلب باز
- **ثبت پرداخت** روی هر خرید (نقدی/حواله/چک/سایر + شماره پیگیری) — سرور ماندهٔ بدهی را کنترل می‌کند (اضافه‌پرداخت ۴۲۲)
- **هشدار سررسید گذشته**: بج قرمز روی خرید عقب‌افتاده + فهرست هشدار + کارت داشبورد
- **طلب از کارفرما** (دریافت‌ها): ثبت شرح/مبلغ/سررسید وصول + وصول/بازگشایی
- ویرایش سررسید و شرایط پرداخت توسط حسابدار/مدیر کل

### انبار کارگاه (جدید)
- موجودی به تفکیک کارگاه × مصالح × واحد؛ ورود خودکار از دریافت خرید و تأیید انبار؛ خروج دستی انباردار
- حداقل موجودی → نشانگر «کمبود» + کارت داشبورد + اعلان به انباردارها

### گزارش کار کارگران + ارزیابی و KPI
- سرپرست/مدیر: ثبت «چه کسی، چه روزی، چه کاری» + تعداد نفرات گروه
- **نیروی اجرایی (کارگر با حساب سامانه): خوداظهاری گزارش کارش — نام/کارگاه از حسابش اجباری می‌شود**
- مدیر/مدیر کل: فهرست با فیلتر بازهٔ تاریخ و جستجو — بدانید کارگرانتان چه می‌کنند
- **ارزیابی و KPI**: شرح وظایف هر کارگر + هدف ماهانه + **درصد تحقق** و عملکرد (تعداد گزارش، نفر-روز) + تجمیع دوره‌ای کارگاه

### بایگانی
- ثبت‌های بسته‌شده با چیپ «بایگانی» در فهرست ثبت‌ها در دسترس‌اند

## ساختار مهم

- `src/components/app/` — رابط کاربری (تمام صفحات، تقویم جلالی پاپ‌آپ: `jalali-calendar-sheet.tsx`، پد امضا: `signature-pad.tsx`)
- `src/lib/fa.ts` — توابع تقویم جلالی و فارسی‌سازی ارقام
- `src/app/api/v1/` — API لایه‌بندی‌شده (auth، material-entries، statements، purchase-requests، work-reports، daily-*، admin، ...)
- `prisma/schema.prisma` — مدل داده (Migration ۵: مالی ساده و KPI)
- `uploads/` — فایل‌های خصوصی (خارج از public؛ فقط با Signed URL)
- `docs/ARCHITECTURE.md` — معماری کامل (State Machine، ماتریس دسترسی، Scope Chain)
- `docs/INTEGRATED-SYSTEM.md` — نگاشت کامل سند PDF به ماژول‌های پیاده‌شده
- `download/` — بستهٔ تحویل (راهنما، معماری، اسکرین‌شات‌های راستی‌آزمایی)

## استقرار تولید

```bash
bun run build
bun run start
```

یا با Docker:

```bash
cp .env.example .env   # مقادیر Secret را پر کنید
docker compose up -d --build
```

جزئیات کامل معماری و امنیت: `download/README.md` و `docs/ARCHITECTURE.md`

## پشتیبان‌گیری
```bash
npm run db:backup   # VACUUM INTO (SQLite) یا pg_dump (PostgreSQL)
npm run db:restore  # RESTORE_FILE=backups/db-….db npm run db:restore
```
